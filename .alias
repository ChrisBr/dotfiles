# OSC CLI aliases
# Internal osc build.suse.de
alias iosc='osc -A iosc'
# Development osc localhost:3000
alias tosc='osc -A tosc'

# Directory navigation aliases
alias ..='cd ..'
alias ....='cd ../../..'
alias .....='cd ../../../..'
alias h='cd'
alias la='ls -la'

alias g='git'
alias gti='git'
alias be="bundle exec"
alias mkdir="mkdir -p"

# Uberspace
# sudo sshuttle -r chrisbr@pictor.uberspace.de 0.0.0.0/0 -vv
# https://openid.stackexchange.com - christian@bruckmayer.net
alias uberspace='ssh chrisbr@fenrir.uberspace.de'
alias tanja="ssh tanja@barnard.uberspace.de"
alias sck="ssh sck@whipple.uberspace.de"

# Docker
drm() { docker rm $(docker ps -a -q); }
dka() { docker kill $(docker ps -q); }

alias rlias=". ~/.alias"
alias whatsmyip='curl -s https://am.i.mullvad.net/json | jq'

alias config='/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME'

